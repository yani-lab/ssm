<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zking.ssm.mapper.info.TRealauthMapper">
    <insert id="addRealauth" parameterType="com.zking.ssm.model.info.TRealauth">
        insert into t_realauth (realname, sex, bornDate, idNumber, address, state, image1, image2, remark, applyTime,  applier_id)
        values (#{realname},#{sex},#{bornDate},#{idNumber},#{idNumber},#{address},#{state},#{image1},#{image2},#{remark},#{applyTime},#{applier_id})
    </insert>
</mapper>